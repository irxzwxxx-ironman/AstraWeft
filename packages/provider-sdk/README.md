# AstraWeft Provider SDK

Public, independently versioned contracts for trusted local AstraWeft Provider plugins.

Plugins depend on this package—not on AstraWeft Core, Qt, SQLAlchemy, or private implementation
modules. The API is pre-1.0 while the Mock Provider and first real integrations validate it.
