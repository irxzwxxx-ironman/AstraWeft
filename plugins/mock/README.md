# AstraWeft Mock Provider

Deterministic, zero-network Provider used to validate Core, GUI, failure handling, and the public
Provider contract before any paid service integration. It imports only `astraweft-provider-sdk`.

The credential value used in local development is `mock-valid-key`; it is test data, not a secret.
