# AstraWeft Runway Provider

Independent AstraWeft Provider plugin for Runway asynchronous text-to-video tasks.

The first slice supports the `gen4.5` model, durable submit/poll/cancel behavior,
and Core-owned local materialization of ephemeral output URLs. Tests are offline
and do not make paid API calls.
